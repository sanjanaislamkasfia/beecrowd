#include<bits/stdc++.h>
#include<iostream>
int main() {
    int N;
    std::cin >> N;
    for (int i = 2; i <= N; i += 2) {
        std::cout << i << "^2 = " << i * i << std::endl;
    }

    return 0;
}
